feign:
  httpclient:
    connection-timeout: 30000
# spring配置
spring:
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB
  redis:
    host: 192.168.52.164
    port: 6379
    password: shengtong
  datasource:
    url: jdbc:postgres_jts://192.168.160.105:5432/pathmedics
    username: postgres
    password: 123456
    driver-class-name: org.locationtech.jts.jdbc.JtsWrapper
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 300000
      max-lifetime: 1800000
      connection-timeout: 5000
      validation-timeout: 5000
      auto-commit: true
      pool-name: "annotaionHikariCP"
      connection-test-query: "SELECT 1"
      register-mbeans: true
    
# mybatis配置
mybatis:
  # 搜索指定包别名
  typeAliasesPackage: cn.staitech.annotation
  # 配置mapper的扫描，找到所有的mapper.xml映射文件
  mapperLocations: classpath:mapper/**/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    # log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl
# swagger配置
swagger:
  title: 标注模块接口文档
  license: Powered By staitech
  licenseUrl: http://www.staitech.cn

geometry:
  insideMaxArea: 1000.0

netty:
  port: 7777
  # port: 9999